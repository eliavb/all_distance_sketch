/*! \file cover.proto
    \brief Format to save the cover
*/
syntax = "proto2";

package all_distance_sketch.proto;


message NodeEmbeddingGpb {
  optional uint64 node_id = 1;
  repeated double coordinate = 2;
}

message EmbeddingGpb {
  repeated NodeEmbeddingGpb nodes = 1;
}